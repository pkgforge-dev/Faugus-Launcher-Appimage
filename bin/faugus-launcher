#!/bin/sh

case "$1" in
	--shortcut) exec python3 -m faugus.shortcut "$2";;
	--game)     exec python3 -m faugus.runner --game "$2";;
	--run)      exec python3 -m faugus.runner "$2";;
	*)          exec python3 -m faugus.launcher "$1";;
esac
